import styled from 'styled-components';

export const IntegrationButton = styled.a`
    display: inline-block;
    padding: 12px;
    box-shadow: 0 2px 5px #00000030;
    border-radius: 4px;
    :hover {
        transform: scale(1.1);
    }
`;
